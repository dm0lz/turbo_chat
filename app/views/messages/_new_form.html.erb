<%= turbo_frame_tag 'new_message' do %>
  <%= form_with(model: message, url: room_messages_path(room_id: message.room.id, format: :turbo_stream)) do |form| %>
    <%= render 'messages/form', message: message, form: form %>
  <% end %>
<% end %>


<%#= link_to 'Back', messages_path %>
